# App source

This is the React/Vite source for the site. See the [repo root README](../README.md) for the full picture — local dev, build/deploy workflow, and how the 3D layer is structured.

Quick start:

```bash
npm install
npm run dev
```
